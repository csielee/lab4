lab4 李東霖 F74031027

A.
00000000004004b4 T _Z7averagePdRd
00000000004004e2 T _Z7averageif

T is encoded identifiers are used for the functions

B.
顯示結果：
1 8
4 8
4 8
8 8

sizeof()會顯示該變數所使用的位元數(bit)，char是1byte，int是4bytes，float是4bytes，double是8bytes
後面都是8bytes是因為變數的指標都是代表記憶體位址，所以使用位元數都會相等，大小會是記憶體大小
